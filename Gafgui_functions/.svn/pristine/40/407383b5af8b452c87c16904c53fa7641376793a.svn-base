% --------------------------------------------------------------------
function s = fct_addbackslash(strg)

l = length(strg);
s=[];
for i=1:l
    if strg(i)=='_'
        s = [s '\'];
    end
    s = [s strg(i)];
end